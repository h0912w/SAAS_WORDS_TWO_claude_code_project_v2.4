# PROJECT PLAYBOOK

**2026-08-19 정리(사용자 지시로 전체 문서 감사 후)**: 이 파일은 원래 옛
수요/공급 파이프라인(`DEMAND-001`, 2026-08-18에 코드·문서·테스트까지 완전
삭제됨)의 실험 기록으로 거의 전부 채워져 있었다. 그 데이터원/군집화 관련
내용은 지금 남아있는 어떤 코드에도 적용되지 않고, `memory/ACTIVE_ISSUES.md`의
`DEMAND-001` 항목에 이미 그대로 보존돼 있어(CLAUDE.md 1장 참고) 여기 다시
남겨둘 필요가 없다 — 중복 제거 목적으로 삭제했다. 아래는 **지금 파이프라인
(단어뱅크 기반 제목 생성, `word_pipeline.py`)에도 여전히 적용되는 일반
원칙만** 남긴 것이다. 단어 생성 자체의 시행착오는 이제
`memory/WORD_GENERATION_LEARNINGS.md`가 전담한다(이 파일과 역할이 겹치지
않게 분리됨).

## validated
- 결정론적 수집·정리·점수·중복·게시 작업은 코드로 처리한다.
- 의미 해석이 필요한 소수 항목만 현재 Claude Code 세션과 서브에이전트가 판정한다.
- QA는 운영과 동일한 진입점과 코드 경로를 사용한다.
- 운영 이력은 원자적으로 갱신하고 QA에서는 수정하지 않는다.

## validated (2026-08-11 프로젝트 정의 전환에서 확인, 지금도 유효)
- **프로젝트의 근본 정의(CLAUDE.md 1장)를 바꾸는 지시는, 실제 구현에 들어가기
  전에 AskUserQuestion으로 최소 2가지를 먼저 확인할 것: (1) 구조 — 완전 전환인지
  순서만 바뀌는지 병행인지, (2) 발행 기준 — 검증 없이 그대로 최종 산출물로
  써도 되는지.** 이 두 축만 확인해도 구현 방향이 크게 갈린다. "이전 지시(자율
  진행)가 있었으니 이번에도 확인 없이 진행"하지 말고, **작업 범위 자체가
  달라지는 지시는 매번 새로 판단**할 것.
- **기존 코드/모듈을 "당장 안 쓴다"는 이유로 먼저 삭제하지 말고 보존할 것 —
  삭제는 사용자가 명시적으로 지시했을 때만 한다.** (2026-08-18 수요/공급
  파이프라인 전체 삭제, 2026-08-19 이 문서의 DEMAND-001 실험 기록 삭제 모두
  사용자의 명시적 지시 이후에 실행됐다 — 이 원칙과 모순되지 않는다.)
- 새 파이프라인을 만들 때는 기존 모듈에서 재사용 가능한 순수 함수/클래스를
  먼저 식별하고 그대로 import해서 쓸 것 — 특정 도메인 개념(예: 옛 "기회"
  개념)에 실제로 의존하는 로직인지, 아니면 dict 키만 보는 순수 로직인지
  구분해두면 나중에 재사용이 쉬워진다.

## validated (2026-08-26, Google Ads 자격증명 교체 실측에서 확인)
- **Google Ads API 자격증명(REFRESH_TOKEN/CUSTOMER_ID/LOGIN_CUSTOMER_ID/
  DEVELOPER_TOKEN)을 새로 설정하거나 교체할 때는 반드시
  `docs/operations/15-google-ads-credential-setup.md`부터 읽을 것** — 다섯 가지
  서로 다른 원인(폐쇄된 계정을 테스트 계정으로 오인, 프로덕션 관리자 밑에 테스트
  계정 생성 시도, OAuth 호출 사용자와 developer-token 소유자 혼동, OAuth 앱 테스터
  미등록, 개발자 토큰의 GCP 프로젝트 영구 페어링)을 실측으로 겪고 공식 문서
  근거와 함께 정리해뒀다 — 같은 시행착오를 반복하지 않기 위한 문서다.
- **UI 화면에 보이는 라벨(예: "관리자", "폐쇄")만으로 계정 성격을 단정하지 말고,
  공식 문서(developers.google.com)나 실제 API 호출 결과로 교차검증할 것** —
  이번 배치에서 "관리자(폐쇄)" 라벨을 실제 폐기로 오판할 뻔했으나, 진짜 테스트
  계정에는 별도로 빨간 "테스트 계정" 배지가 뜬다는 근거로 구분해 냈다.
